extends layout

block content
  form(method="post", action="/form/1")
    div
      h5 メンバー(改行して複数入力してください)
      textarea(id="members" name="members_field")
    div
      h5 曲名(改行して複数入力してください)
      textarea(id="pieces" name="pieces_field")
    button(type="submit") 次へ